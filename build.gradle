plugins {
    id 'java'
    id 'eclipse'
    id 'application'
}

mainClassName = 'mansolsson.sudoku.resolver.Gui'

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.12'
}

task wrapper(type: Wrapper) {
	gradleVersion = '3.1'
}
